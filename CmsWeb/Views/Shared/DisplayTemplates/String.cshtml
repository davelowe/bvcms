@using UtilityExtensions
@model string
<div class="form-group">
    @Html.LabelFor(m => m, new { @class="control-label"})
    <div class="controls">
        <p>@Html.Raw(Model.HasValue() ? Model : "&nbsp;")</p>
    </div>
</div>
